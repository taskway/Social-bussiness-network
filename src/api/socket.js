/*
import io from 'socket.io-client'
const socket = io('http://192.168.1.47:3001')

export const messagesAPI = {
    addNewMessage(message, dialogId, userId, userName, currentUserId) {
        socket.emit('messageFromFrontend', {
            message,
            dialogId,
            userId,
            userName,
            currentUserId
        })
    }
}

export default socket;*/
